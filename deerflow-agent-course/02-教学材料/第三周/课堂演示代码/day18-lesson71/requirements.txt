# OAuth集成演示代码依赖包
# 版本要求基于Python 3.12+

# 核心依赖
httpx>=0.27.0  # 异步HTTP客户端，用于OAuth API调用

# 测试依赖（可选）
pytest>=8.0.0  # 测试框架
pytest-asyncio>=0.23.0  # 异步测试支持
pytest-cov>=5.0.0  # 测试覆盖率

# 开发工具
black>=24.0.0  # 代码格式化
isort>=5.13.0  # 导入排序
mypy>=1.8.0  # 类型检查
flake8>=7.0.0  # 代码检查

# 可选：生产环境增强
pyjwt>=2.8.0  # JWT令牌支持（PKCE扩展）
cryptography>=42.0.0  # 加密库（令牌安全存储）
redis>=5.0.0  # 分布式令牌存储
pydantic>=2.5.0  # 配置验证

# 安装所有依赖：
# pip install -r requirements.txt

# 仅安装核心依赖：
# pip install httpx>=0.27.0