# 类解析演示代码依赖
# 运行演示代码所需的最小依赖集

# 注意：Python 3.12+ 要求，因为使用了最新的类型注解和异步特性
# 所有依赖都是标准库模块，无需额外安装

# 可选测试依赖（用于运行独立测试）
pytest>=8.0.0
pytest-asyncio>=0.23.0

# 可选数据类扩展（用于更复杂的配置）
# dataclasses-json>=0.6.0  # 如果需要JSON序列化支持

# 可选依赖注入框架（用于扩展功能）
# dependency-injector>=4.40.0  # 如果需要完整的DI容器

# 可选性能分析工具（用于基准测试）
# memory-profiler>=0.61.0  # 如果需要内存分析
# pyinstrument>=4.0.0      # 如果需要性能分析

# 注意：本演示代码主要使用Python标准库，不需要额外依赖
# 如果需要运行测试，请安装pytest和pytest-asyncio