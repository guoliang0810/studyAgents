# 配置驱动加载演示代码依赖
# 运行演示代码所需的最小依赖集

# 注意：Python 3.12+ 要求，因为使用了最新的类型注解和异步特性

# 核心依赖（YAML配置文件支持）
PyYAML>=6.0.0

# 可选测试依赖（用于运行独立测试）
pytest>=8.0.0
pytest-asyncio>=0.23.0

# 可选配置管理扩展
# python-json-config>=1.2.0  # 如果需要JSON配置增强
# dynaconf>=3.2.0          # 如果需要动态配置管理

# 可选依赖注入框架（用于扩展功能）
# dependency-injector>=4.40.0  # 如果需要完整的DI容器

# 可选性能分析工具（用于基准测试）
# memory-profiler>=0.61.0  # 如果需要内存分析
# pyinstrument>=4.0.0      # 如果需要性能分析

# 可选热重载支持
# watchdog>=3.0.0          # 如果需要文件监视热重载

# 注意：除了PyYAML外，其他都是标准库模块
# 如果需要运行测试，请安装pytest和pytest-asyncio
# 如果需要YAML配置文件支持，请安装PyYAML