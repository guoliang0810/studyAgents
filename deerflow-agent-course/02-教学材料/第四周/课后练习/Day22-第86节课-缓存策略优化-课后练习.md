# 🎯 Day22 第86节课：缓存策略优化 - 课后练习

## 📋 练习概述
- **课程**: Day22 第86节课：缓存策略优化
- **主题**: 在DeerFlow中实现高效的缓存策略
- **建议完成时间**: 60分钟
- **难度分布**: 基础30% | 进阶40% | 挑战30%

## 📚 练习目标
通过本练习，你将能够：
1. 理解缓存的基本原理和策略
2. 实现多级缓存架构
3. 掌握缓存失效策略
4. 优化Agent系统的缓存命中率

## 🧠 概念理解题（建议时间：15分钟）

### 练习1：缓存策略类型
**题目**: 解释以下缓存策略：Cache-Aside、Read-Through、Write-Through、Write-Behind。

**要求**:
1. 说明每种策略的工作原理
2. 分析各策略的优缺点
3. 说明适用场景

**知识点**: 缓存策略、系统设计
**难度**: 基础
**建议时间**: 10分钟

---

### 练习2：缓存失效问题
**题目**: 解释缓存击穿、缓存穿透、缓存雪崩的概念，并提出解决方案。

**要求**:
1. 用自己的话解释每个概念
2. 针对每种问题提供解决方案
3. 分析哪种问题对Agent系统影响最大

**知识点**: 缓存问题、系统稳定性
**难度**: 进阶
**建议时间**: 10分钟

---

## 💻 代码实现题（建议时间：30分钟）

### 练习3：实现缓存装饰器
**题目**: 实现一个通用的缓存装饰器，支持TTL和最大缓存大小。

**要求**:
1. 支持设置过期时间(TTL)
2. 支持设置最大缓存条目数
3. 支持手动失效缓存
4. 线程安全

**代码框架**:
```python
import time
import threading
from functools import wraps
from typing import Any, Callable, Optional, Dict

class CacheEntry:
    """缓存条目"""
    def __init__(self, value: Any, ttl: float):
        self.value = value
        self.expire_time = time.time() + ttl
    
    def is_expired(self) -> bool:
        return time.time() > self.expire_time

class TTLCache:
    """带TTL的线程安全缓存"""
    
    def __init__(self, max_size: int = 100, default_ttl: float = 300):
        self.max_size = max_size
        self.default_ttl = default_ttl
        self._cache: Dict[str, CacheEntry] = {}
        self._lock = threading.Lock()
    
    def get(self, key: str) -> Optional[Any]:
        """获取缓存"""
        pass
    
    def set(self, key: str, value: Any, ttl: Optional[float] = None):
        """设置缓存"""
        pass
    
    def delete(self, key: str):
        """删除缓存"""
        pass
    
    def clear(self):
        """清空缓存"""
        pass

def cached(cache: TTLCache, key_func: Optional[Callable] = None):
    """缓存装饰器"""
    def decorator(func: Callable):
        @wraps(func)
        def wrapper(*args, **kwargs):
            # 生成缓存键
            if key_func:
                cache_key = key_func(*args, **kwargs)
            else:
                cache_key = f"{func.__name__}:{str(args)}:{str(kwargs)}"
            
            # 尝试从缓存获取
            result = cache.get(cache_key)
            if result is not None:
                return result
            
            # 执行函数并缓存结果
            result = func(*args, **kwargs)
            cache.set(cache_key, result)
            return result
        return wrapper
    return decorator
```

**知识点**: 缓存装饰器、TTL、线程安全
**难度**: 进阶
**建议时间**: 15分钟

---

### 练习4：实现Agent响应缓存
**题目**: 为DeerFlow Agent实现LLM响应缓存，减少重复请求。

**要求**:
1. 使用缓存存储LLM响应
2. 支持基于提示词哈希的缓存键
3. 实现缓存统计（命中率）
4. 支持缓存失效

**代码框架**:
```python
import hashlib
import json
from typing import Any, Dict, Optional
from langchain_openai import ChatOpenAI

class LLMResponseCache:
    """LLM响应缓存"""
    
    def __init__(self, cache: TTLCache):
        self.cache = cache
        self.hits = 0
        self.misses = 0
    
    def _generate_key(self, prompt: str, model: str, **kwargs) -> str:
        """生成缓存键"""
        content = json.dumps({
            "prompt": prompt,
            "model": model,
            **kwargs
        }, sort_keys=True)
        return hashlib.sha256(content.encode()).hexdigest()
    
    def get(self, prompt: str, model: str, **kwargs) -> Optional[str]:
        """获取缓存的响应"""
        key = self._generate_key(prompt, model, **kwargs)
        result = self.cache.get(key)
        if result:
            self.hits += 1
        else:
            self.misses += 1
        return result
    
    def set(self, prompt: str, response: str, model: str, **kwargs):
        """缓存响应"""
        key = self._generate_key(prompt, model, **kwargs)
        self.cache.set(key, response)
    
    def get_hit_rate(self) -> float:
        """获取缓存命中率"""
        total = self.hits + self.misses
        return self.hits / total if total > 0 else 0.0
```

**知识点**: LLM缓存、哈希键、缓存统计
**难度**: 进阶
**建议时间**: 15分钟

---

## 🔧 实战应用题（建议时间：15分钟）

### 练习5：设计多级缓存架构
**题目**: 为Agent系统设计多级缓存架构（L1本地缓存 + L2分布式缓存）。

**要求**:
1. 分析适合缓存的数据类型
2. 设计缓存层级
3. 实现缓存一致性策略

**知识点**: 多级缓存、分布式系统
**难度**: 挑战
**建议时间**: 15分钟

---

## 📝 思考与分析题

### 练习6：缓存成本效益分析
**题目**: 分析在Agent系统中使用缓存的收益和成本。

**要求**:
1. 列出缓存带来的收益
2. 列出使用缓存的成本
3. 提出缓存策略的选择标准

**知识点**: 缓存策略、成本效益
**难度**: 挑战
**建议时间**: 10分钟
