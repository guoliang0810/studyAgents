# Day23 第91节课：认证与授权 - 课后练习

## 练习概述
- 主题：实现Agent系统的认证与授权
- 建议完成时间：60分钟
- 难度：基础30% | 进阶40% | 挑战30%

## 练习目标
1. 理解认证授权机制
2. 实现JWT认证
3. 实现基于角色的授权

## 概念理解题

### 练习1：认证vs授权
**题目**: 解释认证(Authentication)和授权(Authorization)的区别。

### 练习2：JWT
**题目**: 解释JWT的工作原理和安全性考虑。

## 代码实现题

### 练习3：JWT认证实现
**题目**: 实现JWT认证中间件。

```python
import jwt
from datetime import datetime, timedelta
from functools import wraps
from flask import request, jsonify

SECRET_KEY = "your-secret-key"

class AuthService:
    """认证服务"""
    
    @staticmethod
    def create_token(user_id: str, roles: list) -> str:
        payload = {
            "user_id": user_id,
            "roles": roles,
            "exp": datetime.utcnow() + timedelta(hours=24)
        }
        return jwt.encode(payload, SECRET_KEY, algorithm="HS256")
    
    @staticmethod
    def verify_token(token: str) -> dict:
        try:
            return jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
        except jwt.ExpiredSignatureError:
            return None
```

### 练习4：RBAC授权
**题目**: 实现基于角色的访问控制。

## 实战应用题

### 练习5：设计企业级认证方案
**题目**: 设计支持SSO的认证方案。

## 思考与分析题

### 练习6：Token安全策略
**题目**: 分析Token的安全考虑，如何防止Token泄露？
