# 🎓 Day 25：部署架构与DevOps - 互动教学实录

## 👨‍🏫 讲师介绍
**张老师**：10年AI系统架构经验，DeerFlow核心贡献者

## 👨‍🎓 学生背景
**小王**：Python基础一般，完成了Day 24学习，掌握了插件系统

## 📅 课程时间
- **日期**：2024年4月18日（周四）
- **时间**：上午9:00-12:45（4节课，每节45分钟，休息15分钟）

---

## 🏫 第97节：部署模式（9:00-9:45）

### 复习与导入

**张老师**："大家早上好！今天我们来学习部署架构与DevOps。小王，你知道有哪些部署方式吗？"

**小王**："老师好！有Docker、Kubernetes那些？"

**张老师**："对！让我们学习如何部署Agent系统。"

### 部署模式

```python
print("""
部署模式：

1. 【单体部署】
   简单，适合小规模
   
2. 【微服务部署】
   扩展性好，适合大规模

3. 【Serverless】
   按需扩展，成本优化

4. 【混合部署】
   组合多种模式
""")
```

### Docker部署

```dockerfile
# Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["python", "main.py"]
```

### Docker Compose

```yaml
# docker-compose.yml
version: '3.8'
services:
  deerflow:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    volumes:
      - ./config:/app/config

  redis:
    image: redis:7-alpine
```

---

## ☕ 课间休息（9:45-10:00）

---

## 🏫 第98节：CI/CD流水线（10:00-10:45）

### 流水线设计

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: pytest tests/

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Build Docker image
        run: docker build -t deerflow:${{ github.sha }} .

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: kubectl apply -f k8s/
```

---

## ☕ 课间休息（10:45-11:00）

---

## 🏫 第99节：Kubernetes部署（11:00-11:45）

### K8s配置

```yaml
# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deerflow
spec:
  replicas: 3
  selector:
    matchLabels:
      app: deerflow
  template:
    metadata:
      labels:
        app: deerflow
    spec:
      containers:
      - name: deerflow
        image: deerflow:latest
        ports:
        - containerPort: 8000
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
```

---

## ☕ 课间休息（11:45-12:00）

---

## 🏫 第100节：实战：构建部署流水线（12:00-12:45）

### 综合实战

```python
class DeploymentManager:
    """部署管理器"""
    
    def __init__(self):
        self.environments = ["dev", "staging", "production"]
    
    async def deploy(self, environment: str, version: str):
        """部署到环境"""
        if environment not in self.environments:
            raise ValueError(f"未知环境: {environment}")
        
        print(f"🚀 部署到 {environment} (版本: {version})")
        # 实际部署逻辑
        return True
    
    async def rollback(self, environment: str):
        """回滚"""
        print(f"⏪ 回滚 {environment}")
        return True

### 课程总结

**张老师**："第二十五天的课程就到这里！我们来回顾一下：

1. **部署模式**：Docker、K8s
2. **CI/CD**：自动化流水线
3. **K8s部署**：容器编排
4. **实战**：构建部署系统

小王，你今天最大的收获是什么？"

**小王**："学会了如何部署Agent系统！"

### 课后作业
1. 设计蓝绿部署策略
2. 实现自动回滚机制

### 明日预告
明天我们将学习大规模扩展策略！

---

**张老师寄语**："好的部署让系统更稳定。今天你学会了如何部署Agent系统，明天我们将学习如何大规模扩展！"
