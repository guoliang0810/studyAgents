# 🎓 Day 28：多租户与企业集成 - 互动教学实录

## 👨‍🏫 讲师介绍
**张老师**：10年AI系统架构经验，DeerFlow核心贡献者

## 👨‍🎓 学生背景
**小王**：Python基础一般，完成了Day 27学习，掌握了高可用架构

## 📅 课程时间
- **日期**：2024年4月21日（周日）
- **时间**：上午9:00-12:45（4节课，每节45分钟，休息15分钟）

---

## 🏫 第109节：多租户架构（9:00-9:45）

### 复习与导入

**张老师**："大家早上好！今天我们来学习多租户与企业集成。小王，什么是多租户？"

**小王**："老师好！就是多个客户共享一个系统？"

**张老师**："对！但数据要隔离。"

### 租户隔离

```python
@dataclass
class Tenant:
    """租户"""
    id: str
    name: str
    plan: str  # free, pro, enterprise
    quota: Dict[str, int]

class TenantManager:
    """租户管理器"""
    
    def __init__(self):
        self.tenants: Dict[str, Tenant] = {}
        self.current_tenant: Optional[str] = None
    
    def create_tenant(self, name: str, plan: str = "free") -> Tenant:
        """创建租户"""
        tenant = Tenant(
            id=f"tenant_{uuid.uuid4().hex[:8]}",
            name=name,
            plan=plan,
            quota=self._get_quota(plan)
        )
        self.tenants[tenant.id] = tenant
        return tenant
    
    def _get_quota(self, plan: str) -> Dict[str, int]:
        """获取配额"""
        quotas = {
            "free": {"requests_per_day": 100, "max_concurrent": 1},
            "pro": {"requests_per_day": 10000, "max_concurrent": 5},
            "enterprise": {"requests_per_day": -1, "max_concurrent": -1}
        }
        return quotas.get(plan, quotas["free"])
    
    def set_current_tenant(self, tenant_id: str):
        """设置当前租户"""
        if tenant_id not in self.tenants:
            raise ValueError(f"租户不存在: {tenant_id}")
        self.current_tenant = tenant_id
    
    def check_quota(self) -> bool:
        """检查配额"""
        if not self.current_tenant:
            return True
        
        tenant = self.tenants[self.current_tenant]
        # 实际配额检查逻辑
        return True
```

---

## ☕ 课间休息（9:45-10:00）

---

## 🏫 第110节：数据隔离策略（10:00-10:45）

### 隔离模式

```python
class TenantIsolation:
    """租户数据隔离"""
    
    def __init__(self):
        self.mode = "schema"  # schema, database, row-level
    
    def get_tenant_filter(self, tenant_id: str) -> str:
        """获取租户过滤条件"""
        if self.mode == "row-level":
            return f"tenant_id = '{tenant_id}'"
        return ""
    
    def create_tenant_schema(self, tenant_id: str):
        """为租户创建独立schema"""
        if self.mode == "schema":
            print(f"📦 创建租户schema: tenant_{tenant_id}")
```

---

## ☕ 课间休息（10:45-11:00）

---

## 🏫 第111节：企业集成模式（11:00-11:45）

### SSO集成

```python
class SSOIntegration:
    """SSO集成"""
    
    def __init__(self):
        self.providers = {}
    
    def add_provider(self, name: str, config: Dict):
        """添加SSO提供商"""
        self.providers[name] = config
        print(f"🔐 添加SSO提供商: {name}")
    
    async def authenticate(self, provider: str, token: str) -> Dict:
        """SSO认证"""
        if provider not in self.providers:
            raise ValueError(f"未知提供商: {provider}")
        
        # 实际认证逻辑
        return {"user_id": "user123", "tenant_id": "tenant123"}
```

---

## ☕ 课间休息（11:45-12:00）

---

## 🏫 第112节：API网关集成（12:00-12:45）

### 网关配置

```python
class APIGateway:
    """API网关"""
    
    def __init__(self):
        self.routes = {}
        self.middlewares = []
    
    def add_route(self, path: str, service: str, methods: List[str] = None):
        """添加路由"""
        self.routes[path] = {
            "service": service,
            "methods": methods or ["GET", "POST"]
        }
    
    def add_middleware(self, middleware: Callable):
        """添加中间件"""
        self.middlewares.append(middleware)

### 课程总结

**张老师**："第二十八天的课程就到这里！我们来回顾一下：

1. **多租户架构**：隔离和配额
2. **数据隔离**：schema和row-level
3. **SSO集成**：企业认证
4. **API网关**：统一入口

小王，你今天最大的收获是什么？"

**小王**："学会了如何构建企业级多租户系统！"

### 课后作业
1. 实现租户计费系统
2. 设计资源配额管理

### 明日预告
明天我们将学习前沿技术与未来展望！

---

**张老师寄语**："企业集成让系统更强大。今天你学会了如何构建多租户系统，明天我们将展望未来！"
