# 🎓 Day 5：工具错误处理与用户交互 - 互动教学实录

## 👨‍🏫 讲师介绍
**张老师**：10年AI系统架构经验，DeerFlow核心贡献者

## 👨‍🎓 学生背景
**小王**：Python基础一般，前四天学习掌握了中间件模式

## 📅 课程时间
- **日期**：2024年3月29日（周五）
- **时间**：上午9:00-12:45（4节课，每节45分钟，休息15分钟）

---

## 🏫 第17节：ToolErrorHandlingMiddleware（9:00-9:45）

### 复习与导入

**张老师**："大家早上好！昨天我们学习了中间件模式，今天我们来学习让Agent更可靠的关键技术：错误处理。小王，Agent执行工具时可能会遇到什么错误？"

**小王**："老师好！可能网络超时、权限不足、参数错误等。"

**张老师**："完全正确！ToolErrorHandlingMiddleware就是处理这些错误的。想象一下飞机的自动驾驶系统，遇到问题时不是坠毁，而是自动切换到安全模式。"

### 错误类型分类

**张老师**："让我们先了解工具可能出现的错误类型："

```python
# 错误类型分类
print("""
工具错误分类：

1. 【执行错误】
   • 超时错误：工具执行时间超过限制
   • 资源错误：内存不足、磁盘空间不足
   • 权限错误：没有文件访问权限
   • 依赖错误：缺少必要的库或工具

2. 【参数错误】
   • 类型错误：参数类型不匹配
   • 范围错误：参数超出有效范围
   • 缺失错误：缺少必需参数
   • 格式错误：参数格式不正确

3. 【业务错误】
   • 业务逻辑错误：操作违反业务规则
   • 状态错误：系统状态不支持该操作
   • 冲突错误：资源被占用或冲突

4. 【外部错误】
   • 网络错误：API调用失败
   • 第三方服务错误：外部依赖不可用
   • 超时错误：等待外部响应超时
""")
```

### 错误处理策略

**张老师**："DeerFlow使用多种错误处理策略："

```python
from enum import Enum
from typing import Optional, Any

class ErrorSeverity(Enum):
    """错误严重级别"""
    LOW = "low"        # 可忽略，不影响执行
    MEDIUM = "medium"  # 警告，继续执行
    HIGH = "high"      # 错误，需要处理
    CRITICAL = "critical"  # 严重错误，终止执行

class ErrorCategory(Enum):
    """错误类别"""
    TIMEOUT = "timeout"
    PERMISSION = "permission"
    PARAMETER = "parameter"
    NETWORK = "network"
    RESOURCE = "resource"
    UNKNOWN = "unknown"

@dataclass
class ToolError:
    """工具错误"""
    tool_name: str
    error_message: str
    category: ErrorCategory
    severity: ErrorSeverity
    retry_count: int = 0
    original_error: Optional[Exception] = None

class ErrorHandlingStrategy(Enum):
    """错误处理策略"""
    RETRY = "retry"              # 重试
    FALLBACK = "fallback"        # 回退到备选方案
    SKIP = "skip"               # 跳过该操作
    ABORT = "abort"             # 终止执行
    NOTIFY = "notify"            # 通知用户

# 错误处理策略映射
ERROR_STRATEGIES = {
    ErrorCategory.TIMEOUT: ErrorHandlingStrategy.RETRY,
    ErrorCategory.NETWORK: ErrorHandlingStrategy.RETRY,
    ErrorCategory.PERMISSION: ErrorHandlingStrategy.ABORT,
    ErrorCategory.PARAMETER: ErrorHandlingStrategy.ABORT,
    ErrorCategory.RESOURCE: ErrorHandlingStrategy.FALLBACK,
    ErrorCategory.UNKNOWN: ErrorHandlingStrategy.NOTIFY,
}

def determine_strategy(error: ToolError) -> ErrorHandlingStrategy:
    """根据错误类型确定处理策略"""
    base_strategy = ERROR_STRATEGIES.get(error.category, ErrorHandlingStrategy.NOTIFY)
    
    # 根据严重级别调整策略
    if error.severity == ErrorSeverity.CRITICAL:
        return ErrorHandlingStrategy.ABORT
    elif error.severity == ErrorSeverity.LOW and base_strategy == ErrorHandlingStrategy.RETRY:
        return ErrorHandlingStrategy.SKIP
    
    return base_strategy
```

### ToolErrorHandlingMiddleware实现

**张老师**："现在让我们实现ToolErrorHandlingMiddleware："

```python
import asyncio
from typing import Dict, Any, Optional

class ToolErrorHandlingMiddleware:
    """工具错误处理中间件"""
    
    def __init__(
        self,
        max_retries: int = 3,
        retry_delay: float = 1.0,
        enable_fallback: bool = True
    ):
        self.name = "tool_error_handling"
        self.max_retries = max_retries
        self.retry_delay = retry_delay
        self.enable_fallback = enable_fallback
        self.error_log = []
    
    def classify_error(self, error: Exception) -> ToolError:
        """分类错误"""
        error_msg = str(error).lower()
        
        # 根据错误信息判断类别
        if "timeout" in error_msg or "timed out" in error_msg:
            category = ErrorCategory.TIMEOUT
            severity = ErrorSeverity.MEDIUM
        elif "permission" in error_msg or "denied" in error_msg:
            category = ErrorCategory.PERMISSION
            severity = ErrorSeverity.HIGH
        elif "parameter" in error_msg or "argument" in error_msg or "invalid" in error_msg:
            category = ErrorCategory.PARAMETER
            severity = ErrorSeverity.MEDIUM
        elif "network" in error_msg or "connection" in error_msg:
            category = ErrorCategory.NETWORK
            severity = ErrorSeverity.MEDIUM
        elif "memory" in error_msg or "disk" in error_msg or "space" in error_msg:
            category = ErrorCategory.RESOURCE
            severity = ErrorSeverity.HIGH
        else:
            category = ErrorCategory.UNKNOWN
            severity = ErrorSeverity.MEDIUM
        
        return ToolError(
            tool_name="unknown",
            error_message=str(error),
            category=category,
            severity=severity,
            original_error=error
        )
    
    async def handle_error(
        self,
        error: ToolError,
        context: Dict[str, Any]
    ) -> Dict[str, Any]:
        """处理错误"""
        strategy = determine_strategy(error)
        
        print(f"🔧 [{self.name}] 处理错误: {error.category.value} -> {strategy.value}")
        
        # 记录错误
        self.error_log.append(error)
        
        # 根据策略处理
        if strategy == ErrorHandlingStrategy.RETRY:
            if error.retry_count < self.max_retries:
                error.retry_count += 1
                print(f"🔄 [{self.name}] 重试 ({error.retry_count}/{self.max_retries})")
                await asyncio.sleep(self.retry_delay * error.retry_count)
                return {"action": "retry", "error": error}
            else:
                print(f"⚠️ [{self.name}] 重试次数耗尽")
                return {"action": "fallback", "error": error}
        
        elif strategy == ErrorHandlingStrategy.FALLBACK:
            if self.enable_fallback:
                print(f"🔀 [{self.name}] 切换到备用方案")
                return {"action": "fallback", "error": error}
            else:
                return {"action": "abort", "error": error}
        
        elif strategy == ErrorHandlingStrategy.SKIP:
            print(f"⏭️ [{self.name}] 跳过该操作")
            return {"action": "skip", "error": error}
        
        elif strategy == ErrorHandlingStrategy.ABORT:
            print(f"🛑 [{self.name}] 终止执行")
            return {"action": "abort", "error": error}
        
        elif strategy == ErrorHandlingStrategy.NOTIFY:
            print(f"📢 [{self.name}] 通知用户")
            return {"action": "notify", "error": error}
        
        return {"action": "unknown", "error": error}
    
    def wrap_tool_call(
        self,
        tool_name: str,
        tool_args: Dict,
        tool_func
    ):
        """包装工具调用"""
        async def wrapped_call():
            try:
                result = await tool_func(**tool_args)
                return {"success": True, "result": result}
            except Exception as e:
                error = self.classify_error(e)
                error.tool_name = tool_name
                return await self.handle_error(error, {"tool_name": tool_name, "args": tool_args})
        
        return wrapped_call

# 测试错误处理
async def test_error_handling():
    middleware = ToolErrorHandlingMiddleware(max_retries=2)
    
    # 模拟工具函数
    call_count = 0
    
    async def unreliable_tool(x: int) -> int:
        nonlocal call_count
        call_count += 1
        if call_count < 3:
            raise TimeoutError("工具执行超时")
        return x * 2
    
    # 包装工具调用
    wrapped = middleware.wrap_tool_call(
        "double_number",
        {"x": 5},
        unreliable_tool
    )
    
    result = await wrapped()
    print(f"最终结果: {result}")
    print(f"调用次数: {call_count}")

asyncio.run(test_error_handling())
```

**小王**："这个中间件能自动分类错误并选择合适的处理策略！"

**张老师**："对！这就是生产级错误处理的关键。现在请你实现一个参数验证错误处理器。"

### 互动练习

**小王**："参数验证应该检查类型、范围、必填项..."

```python
class ParameterValidationError(Exception):
    """参数验证错误"""
    def __init__(self, param_name: str, message: str):
        self.param_name = param_name
        self.message = message
        super().__init__(f"参数 '{param_name}': {message}")

class ParameterValidator:
    """参数验证器"""
    
    def __init__(self):
        self.errors = []
    
    def validate(
        self,
        params: Dict[str, Any],
        schema: Dict[str, Any]
    ) -> tuple[bool, list]:
        """验证参数"""
        self.errors = []
        
        for param_name, rules in schema.items():
            value = params.get(param_name)
            
            # 检查必填
            if rules.get("required", False) and value is None:
                self.errors.append(ParameterValidationError(
                    param_name, "缺少必需参数"
                ))
                continue
            
            if value is None:
                continue  # 可选参数为空，直接通过
            
            # 检查类型
            expected_type = rules.get("type")
            if expected_type and not isinstance(value, expected_type):
                self.errors.append(ParameterValidationError(
                    param_name, f"类型错误，期望 {expected_type.__name__}，实际 {type(value).__name__}"
                ))
                continue
            
            # 检查范围
            if isinstance(value, (int, float)):
                if "min" in rules and value < rules["min"]:
                    self.errors.append(ParameterValidationError(
                        param_name, f"值 {value} 小于最小值 {rules['min']}"
                    ))
                if "max" in rules and value > rules["max"]:
                    self.errors.append(ParameterValidationError(
                        param_name, f"值 {value} 大于最大值 {rules['max']}"
                    ))
            
            # 检查枚举
            if "enum" in rules and value not in rules["enum"]:
                self.errors.append(ParameterValidationError(
                    param_name, f"值必须在 {rules['enum']} 中"
                ))
        
        return len(self.errors) == 0, self.errors

# 测试验证器
validator = ParameterValidator()
schema = {
    "name": {"required": True, "type": str},
    "age": {"required": False, "type": int, "min": 0, "max": 150},
    "role": {"type": str, "enum": ["admin", "user", "guest"]}
}

# 测试有效参数
valid_params = {"name": "张三", "age": 25, "role": "user"}
is_valid, errors = validator.validate(valid_params, schema)
print(f"有效参数: is_valid={is_valid}, errors={errors}")

# 测试无效参数
invalid_params = {"name": "李四", "age": -5, "role": "unknown"}
is_valid, errors = validator.validate(invalid_params, schema)
print(f"无效参数: is_valid={is_valid}, errors={errors}")
```

**张老师**："非常好！你实现了完整的参数验证逻辑。"

---

## ☕ 课间休息（9:45-10:00）

**张老师**："休息15分钟。小王，错误处理的概念清楚了吗？"

**小王**："清楚了！错误处理有多种策略：重试、回退、跳过、终止、通知。"

**张老师**："总结得很好！好的错误处理让Agent更健壮。"

---

## 🏫 第18节：ClarificationMiddleware（10:00-10:45）

### 什么是澄清机制

**张老师**："欢迎回来！现在我们来学习ClarificationMiddleware。小王，当AI不确定用户意图时应该怎么办？"

**小王**："应该问用户具体想要什么？"

**张老师**："对！这就是澄清机制。想象一下医生问诊：'你哪里不舒服？'——患者说'不舒服'，医生会追问'具体是什么症状？'"

### 澄清触发条件

**张老师**："让我们定义澄清触发条件："

```python
from enum import Enum

class ClarificationTrigger(Enum):
    """澄清触发条件"""
    AMBIGUITY = "ambiguity"           # 意图模糊
    MISSING_REQUIRED = "missing"      # 缺少必要信息
    CONFLICT = "conflict"            # 信息冲突
    TOO_BROAD = "too_broad"         # 请求太宽泛
    UNKNOWN_ENTITY = "unknown"        # 未知实体

@dataclass
class Clarification:
    """澄清请求"""
    trigger: ClarificationTrigger
    question: str
    context: str
    suggestions: List[str] = field(default_factory=list)
    required: bool = True

class ClarificationMiddleware:
    """澄清中间件"""
    
    def __init__(self):
        self.name = "clarification"
        self.clarifications_made = 0
    
    def detect_ambiguity(self, message: str, context: Dict) -> Optional[Clarification]:
        """检测需要澄清的情况"""
        
        # 检测模糊请求
        vague_patterns = [
            "随便", "都可以", "随便吧", "不知道",
            "帮我", "处理一下", "弄一下"
        ]
        
        if any(pattern in message for pattern in vague_patterns):
            return Clarification(
                trigger=ClarificationTrigger.AMBIGUITY,
                question="我需要更具体的指导：",
                context=message,
                suggestions=[
                    "请告诉我具体要做什么",
                    "可以提供更多细节吗",
                    "你想要什么结果"
                ]
            )
        
        # 检测缺失必要信息
        if "帮我" in message or "请" in message:
            # 检查是否有具体的动作对象
            action_words = ["查找", "搜索", "创建", "删除", "修改", "分析"]
            if not any(word in message for word in action_words):
                return Clarification(
                    trigger=ClarificationTrigger.MISSING_REQUIRED,
                    question="请明确你想要的操作：",
                    context=message,
                    suggestions=["查找信息", "创建文件", "分析数据"]
                )
        
        # 检测冲突
        if "但是" in message or "然而" in message:
            return Clarification(
                trigger=ClarificationTrigger.CONFLICT,
                question="我注意到你有冲突的需求：",
                context=message,
                suggestions=["请确认你最终想要什么", "两个都要还是只选一个"]
            )
        
        return None
    
    async def before_agent(
        self,
        request: AgentRequest,
        config: Optional[Dict] = None
    ) -> AgentRequest:
        """检查是否需要澄清"""
        
        clarification = self.detect_ambiguity(request.message, {})
        
        if clarification:
            self.clarifications_made += 1
            print(f"💬 [{self.name}] 需要澄清 ({self.clarifications_made})")
            print(f"   问题: {clarification.question}")
            print(f"   建议: {', '.join(clarification.suggestions)}")
            
            # 在请求中添加澄清标记
            if not request.metadata:
                request.metadata = {}
            request.metadata["needs_clarification"] = True
            request.metadata["clarification"] = clarification
        
        return request
    
    async def after_agent(
        self,
        response: AgentResponse,
        config: Optional[Dict] = None
    ) -> AgentResponse:
        """后置处理"""
        return response

# 测试澄清中间件
async def test_clarification():
    middleware = ClarificationMiddleware()
    
    test_messages = [
        "帮我弄一下",
        "帮我搜索一下相关信息",
        "我想要A但是也想要B",
        "随便吧"
    ]
    
    for msg in test_messages:
        print(f"\n测试消息: '{msg}'")
        request = AgentRequest(user_id="test", message=msg)
        result = await middleware.before_agent(request)
        if result.metadata and result.metadata.get("needs_clarification"):
            print(f"   ⚠️ 需要澄清")
        else:
            print(f"   ✅ 不需要澄清")

asyncio.run(test_clarification())
```

**小王**："这个中间件能检测各种模糊请求！"

**张老师**："对！好的澄清机制能大幅提升用户体验。现在让我们实现一个更智能的澄清生成器。"

### 智能澄清生成

**张老师**："让我们实现一个基于上下文的智能澄清："

```python
from typing import List, Dict, Any

class SmartClarificationGenerator:
    """智能澄清生成器"""
    
    def __init__(self):
        self.intent_patterns = {
            "search": {
                "keywords": ["搜索", "查找", "找", "查"],
                "required_info": ["搜索内容", "搜索范围"],
                "questions": ["你要搜索什么内容？", "在哪个范围内搜索？"]
            },
            "create": {
                "keywords": ["创建", "新建", "生成"],
                "required_info": ["创建类型", "名称"],
                "questions": ["要创建什么类型的文件/记录？", "叫什么名字？"]
            },
            "delete": {
                "keywords": ["删除", "移除"],
                "required_info": ["删除对象"],
                "questions": ["要删除什么？", "确认要删除吗？"]
            },
            "analyze": {
                "keywords": ["分析", "统计", "研究"],
                "required_info": ["分析对象", "分析维度"],
                "questions": ["要分析什么数据？", "从什么角度分析？"]
            }
        }
    
    def detect_intent(self, message: str) -> tuple[str, List[str]]:
        """检测意图和缺失信息"""
        message_lower = message.lower()
        
        for intent, info in self.intent_patterns.items():
            if any(keyword in message for keyword in info["keywords"]):
                # 检测缺失的信息
                missing_info = []
                for required in info["required_info"]:
                    # 简单检测：检查是否有相关信息
                    if not any(word in message for word in required):
                        missing_info.append(required)
                
                return intent, missing_info
        
        return "unknown", []
    
    def generate_clarification(self, message: str, context: Dict) -> Optional[Clarification]:
        """生成澄清问题"""
        intent, missing_info = self.detect_intent(message)
        
        if not missing_info:
            return None  # 不需要澄清
        
        questions = []
        suggestions = []
        
        for info in missing_info:
            # 生成问题
            if info == "搜索内容":
                questions.append("你要搜索什么内容？")
                suggestions.append("提供具体的搜索关键词")
            elif info == "搜索范围":
                questions.append("在什么范围内搜索？")
                suggestions.append("比如'在当前文件'或'在整个项目'")
            elif info == "创建类型":
                questions.append("要创建什么类型？")
                suggestions.append("比如'文件'、'文件夹'、'记录'")
            elif info == "名称":
                questions.append("叫什么名字？")
                suggestions.append("提供具体的名称")
            elif info == "删除对象":
                questions.append("要删除什么？")
                suggestions.append("请提供具体的对象名称")
            elif info == "分析对象":
                questions.append("要分析什么数据？")
                suggestions.append("比如'销售数据'、'用户日志'")
            elif info == "分析维度":
                questions.append("从什么角度分析？")
                suggestions.append("比如'按月份'、'按地区'")
        
        return Clarification(
            trigger=ClarificationTrigger.MISSING_REQUIRED,
            question="\n".join(questions),
            context=message,
            suggestions=suggestions,
            required=True
        )

# 测试智能澄清
generator = SmartClarificationGenerator()

test_cases = [
    "帮我搜索一下",
    "创建文件",
    "分析用户数据"
]

for msg in test_cases:
    print(f"\n消息: '{msg}'")
    clarification = generator.generate_clarification(msg, {})
    if clarification:
        print(f"  问题: {clarification.question}")
        print(f"  建议: {clarification.suggestions}")
    else:
        print("  ✅ 不需要澄清")
```

**张老师**："智能澄清让Agent能主动询问缺失信息！"

---

## ☕ 课间休息（10:45-11:00）

**张老师**："再休息15分钟。小王，澄清机制的概念清楚了吗？"

**小王**："清楚了！当用户请求模糊时，Agent应该主动询问明确需求。"

**张老师**："总结得很好！好的交互设计让用户体验更流畅。"

---

## 🏫 第19节：TitleMiddleware（11:00-11:45）

### 标题生成的作用

**张老师**："欢迎回来！现在我们来学习TitleMiddleware。小王，为什么对话需要标题？"

**小王**："方便查找和整理对话记录？"

**张老师**："完全正确！标题就像书的目录，让我们能快速定位内容。"

### 标题生成实现

**张老师**："让我们实现一个标题生成中间件："

```python
import re
from typing import Optional

class TitleMiddleware:
    """标题生成中间件"""
    
    def __init__(self):
        self.name = "title"
        self.max_title_length = 50
    
    def extract_key_words(self, text: str) -> List[str]:
        """提取关键词"""
        # 移除标点符号
        text = re.sub(r'[^\w\s]', '', text)
        
        # 停用词
        stop_words = {
            '的', '了', '在', '是', '我', '你', '他', '她', '它',
            '这', '那', '什么', '怎么', '如何', '为什么', '帮忙',
            '请', '帮', '一下', '一个', '这个', '那个', '和', '与'
        }
        
        words = text.split()
        keywords = [w for w in words if w not in stop_words and len(w) > 1]
        
        return keywords[:5]
    
    def generate_title(self, message: str, context: Dict) -> str:
        """生成标题"""
        # 提取关键词
        keywords = self.extract_key_words(message)
        
        # 检测意图类型
        if any(word in message for word in ["帮", "请", "帮我"]):
            if any(word in message for word in ["搜索", "查找", "找"]):
                intent = "搜索"
            elif any(word in message for word in ["创建", "新建"]):
                intent = "创建"
            elif any(word in message for word in ["删除", "移除"]):
                intent = "删除"
            elif any(word in message for word in ["修改", "更新"]):
                intent = "修改"
            elif any(word in message for word in ["分析", "统计"]):
                intent = "分析"
            else:
                intent = "请求"
        elif any(word in message for word in ["怎么", "如何", "什么"]):
            intent = "询问"
        else:
            intent = "对话"
        
        # 生成标题
        if keywords:
            title = f"{intent}: {' '.join(keywords[:3])}"
        else:
            # 截取消息前部分
            title = message[:self.max_title_length]
        
        # 限制长度
        if len(title) > self.max_title_length:
            title = title[:self.max_title_length - 3] + "..."
        
        return title
    
    async def before_agent(
        self,
        request: AgentRequest,
        config: Optional[Dict] = None
    ) -> AgentRequest:
        """生成标题"""
        title = self.generate_title(request.message, {})
        
        print(f"📝 [{self.name}] 生成标题: {title}")
        
        if not request.metadata:
            request.metadata = {}
        request.metadata["generated_title"] = title
        
        return request
    
    async def after_agent(
        self,
        response: AgentResponse,
        config: Optional[Dict] = None
    ) -> AgentResponse:
        """保存标题到线程数据"""
        if response.metadata and "generated_title" in response.metadata:
            title = response.metadata["generated_title"]
            print(f"💾 [{self.name}] 保存标题: {title}")
        
        return response

# 测试标题生成
async def test_title():
    middleware = TitleMiddleware()
    
    test_messages = [
        "帮我搜索一下Python异步编程的资料",
        "如何用Python创建web服务器",
        "请帮我分析这个销售数据",
        "随便聊聊"
    ]
    
    for msg in test_messages:
        request = AgentRequest(user_id="test", message=msg)
        result = await middleware.before_agent(request)
        print(f"'{msg}' -> '{result.metadata['generated_title']}'")

asyncio.run(test_title())
```

**小王**："这个中间件能根据消息内容自动生成简短标题！"

**张老师**："对！现在请你设计一个更智能的标题生成器，支持对话历史分析。"

### 互动练习

**小王**："应该结合之前的对话内容来生成更准确的标题..."

```python
class AdvancedTitleMiddleware(TitleMiddleware):
    """高级标题生成中间件"""
    
    def __init__(self):
        super().__init__()
        self.conversation_history = []
    
    def analyze_conversation_flow(self, messages: List[tuple]) -> str:
        """分析对话流程"""
        if not messages:
            return "新对话"
        
        # 统计意图分布
        intents = {"search": 0, "create": 0, "delete": 0, "modify": 0, "analyze": 0, "question": 0}
        
        for role, msg in messages:
            if role == "user":
                msg_lower = msg.lower()
                if any(w in msg_lower for w in ["搜索", "查找", "找"]):
                    intents["search"] += 1
                elif any(w in msg_lower for w in ["创建", "新建"]):
                    intents["create"] += 1
                elif any(w in msg_lower for w in ["删除", "移除"]):
                    intents["delete"] += 1
                elif any(w in msg_lower for w in ["修改", "更新"]):
                    intents["modify"] += 1
                elif any(w in msg_lower for w in ["分析", "统计"]):
                    intents["analyze"] += 1
                elif any(w in msg_lower for w in ["怎么", "如何", "什么", "为什么"]):
                    intents["question"] += 1
        
        # 找出最频繁的意图
        dominant_intent = max(intents.items(), key=lambda x: x[1])
        
        if dominant_intent[1] == 0:
            return "对话"
        
        intent_map = {
            "search": "搜索任务",
            "create": "创建任务",
            "delete": "删除任务",
            "modify": "修改任务",
            "analyze": "分析任务",
            "question": "问答"
        }
        
        return intent_map.get(dominant_intent[0], "对话")
    
    def generate_advanced_title(
        self,
        message: str,
        conversation_history: List[tuple]
    ) -> str:
        """生成高级标题"""
        # 获取当前消息标题
        current_title = self.generate_title(message, {})
        
        # 分析对话流程
        flow = self.analyze_conversation_flow(conversation_history)
        
        # 如果是对话的一部分，添加序号
        if len(conversation_history) > 0:
            return f"[{len(conversation_history) + 1}] {current_title}"
        
        return current_title
    
    async def before_agent(
        self,
        request: AgentRequest,
        config: Optional[Dict] = None
    ) -> AgentRequest:
        """生成标题"""
        # 获取对话历史
        history = []
        if request.metadata and "conversation_history" in request.metadata:
            history = request.metadata["conversation_history"]
        
        title = self.generate_advanced_title(request.message, history)
        
        print(f"📝 [{self.name}] 生成标题: {title}")
        
        if not request.metadata:
            request.metadata = {}
        request.metadata["generated_title"] = title
        
        return request

# 测试高级标题生成
async def test_advanced_title():
    middleware = AdvancedTitleMiddleware()
    
    # 模拟对话历史
    history = [
        ("user", "帮我搜索Python教程"),
        ("assistant", "找到了Python基础教程"),
        ("user", "有没有进阶的"),
    ]
    
    request = AgentRequest(
        user_id="test",
        message="再帮我找找数据分析的",
        metadata={"conversation_history": history}
    )
    
    result = await middleware.before_agent(request)
    print(f"\n对话历史长度: {len(history)}")
    print(f"生成标题: {result.metadata['generated_title']}")

asyncio.run(test_advanced_title())
```

**张老师**："非常好！你实现了结合对话历史的高级标题生成。"

---

## ☕ 课间休息（11:45-12:00）

**张老师**："午饭前最后一节！坚持一下。小王，今天上午的内容清楚了吗？"

**小王**："清楚了！错误处理、澄清机制、标题生成都很实用。"

**张老师**："总结得很好！这些都是提升用户体验的关键技术。"

---

## 🏫 第20节：实战：错误处理和澄清机制（12:00-12:45）

### 项目需求

**张老师**："最后我们来一个实战项目。小王，我们要创建一个完整的错误处理和澄清系统，能：
1. 统一处理各种错误
2. 智能检测需要澄清的情况
3. 生成用户友好的错误提示"

### 设计思路

**小王**："我需要：
1. 统一的错误处理类
2. 澄清检测器
3. 错误消息格式化器"

### 实现代码

**张老师**："让我们一步步实现。"

```python
class ErrorResponseFormatter:
    """错误响应格式化器"""
    
    def __init__(self):
        self.error_templates = {
            ErrorCategory.TIMEOUT: {
                "title": "⏱️ 操作超时",
                "message": "执行时间过长，请稍后重试",
                "action": "如果问题持续存在，请减少请求的数据量"
            },
            ErrorCategory.NETWORK: {
                "title": "🌐 网络错误",
                "message": "网络连接失败",
                "action": "请检查网络连接后重试"
            },
            ErrorCategory.PERMISSION: {
                "title": "🔐 权限不足",
                "message": "您没有执行此操作的权限",
                "action": "请联系管理员获取权限"
            },
            ErrorCategory.PARAMETER: {
                "title": "📝 参数错误",
                "message": "输入参数有问题",
                "action": "请检查输入的参数是否正确"
            },
            ErrorCategory.RESOURCE: {
                "title": "💾 资源不足",
                "message": "系统资源不足",
                "action": "请稍后重试或减少操作规模"
            },
            ErrorCategory.UNKNOWN: {
                "title": "❓ 未知错误",
                "message": "发生了意外错误",
                "action": "请联系技术支持"
            }
        }
    
    def format_error(self, error: ToolError) -> Dict[str, str]:
        """格式化错误响应"""
        template = self.error_templates.get(
            error.category,
            self.error_templates[ErrorCategory.UNKNOWN]
        )
        
        return {
            "title": template["title"],
            "message": template["message"],
            "detail": error.error_message,
            "action": template["action"],
            "category": error.category.value
        }
    
    def format_clarification(self, clarification: Clarification) -> Dict[str, Any]:
        """格式化澄清请求"""
        return {
            "type": "clarification",
            "question": clarification.question,
            "suggestions": clarification.suggestions,
            "required": clarification.required
        }

# 创建完整的错误处理系统
class RobustErrorHandlingSystem:
    """健壮的错误处理系统"""
    
    def __init__(self):
        self.error_middleware = ToolErrorHandlingMiddleware(max_retries=2)
        self.clarification_middleware = ClarificationMiddleware()
        self.title_middleware = TitleMiddleware()
        self.formatter = ErrorResponseFormatter()
    
    async def process_request(self, request: AgentRequest) -> Dict[str, Any]:
        """处理请求"""
        responses = {}
        
        # 1. 澄清检查
        clarification_request = await self.clarification_middleware.before_agent(request)
        if request.metadata and request.metadata.get("needs_clarification"):
            return {
                "type": "clarification",
                "data": self.formatter.format_clarification(
                    request.metadata["clarification"]
                )
            }
        
        # 2. 标题生成
        title_request = await self.title_middleware.before_agent(request)
        responses["title"] = request.metadata.get("generated_title", "无标题")
        
        # 3. 模拟工具执行（可能出错）
        try:
            # 这里模拟工具执行
            responses["type"] = "success"
            responses["content"] = "操作成功完成"
        except Exception as e:
            # 错误处理
            error = self.error_middleware.classify_error(e)
            error.tool_name = "main_tool"
            
            handling_result = await self.error_middleware.handle_error(error, {})
            
            responses["type"] = "error"
            responses["data"] = self.formatter.format_error(error)
            responses["action"] = handling_result["action"]
        
        return responses

# 测试完整系统
async def test_complete_system():
    system = RobustErrorHandlingSystem()
    
    # 测试正常请求
    print("\n=== 测试正常请求 ===")
    normal_request = AgentRequest(
        user_id="user1",
        message="帮我搜索Python教程",
        metadata={}
    )
    result = await system.process_request(normal_request)
    print(f"结果: {result}")
    
    # 测试模糊请求
    print("\n=== 测试模糊请求 ===")
    vague_request = AgentRequest(
        user_id="user1",
        message="帮我弄一下",
        metadata={}
    )
    result = await system.process_request(vague_request)
    print(f"结果: {result}")

asyncio.run(test_complete_system())
```

### 课程总结

**张老师**："第五天的课程就到这里！我们来回顾一下今天学的内容：

1. **ToolErrorHandlingMiddleware**：工具错误处理和重试策略
2. **ClarificationMiddleware**：智能澄清机制
3. **TitleMiddleware**：自动标题生成
4. **实战：错误处理系统**：完整的错误处理和澄清系统

小王，你今天最大的收获是什么？"

**小王**："我学会了如何让Agent优雅地处理错误和不确定情况。"

**张老师**："总结得很好！好的错误处理和交互设计让Agent更可靠、更易用。

### 课后作业

1. **阅读作业**：阅读DeerFlow错误处理源码
2. **实践作业**：实现一个自定义的错误恢复策略
3. **思考作业**：思考如何设计更智能的澄清机制

### 明日预告

明天我们将学习高级中间件与图片处理。

记住：好的错误处理是可靠系统的基石！

---

## 📝 教学反思

### 学生表现
- 小王对错误处理理解良好
- 能独立设计错误分类和处理策略
- 需要更多实际案例练习

### 教学调整
- 增加更多错误场景案例
- 提供错误日志分析实践
- 增加国际化错误消息处理

### 学习建议
1. **多实践**：模拟各种错误场景
2. **多思考**：设计优雅的错误处理
3. **多总结**：积累常见错误处理模式
4. **多测试**：编写错误处理测试用例

---

**张老师寄语**："错误不可避免，但我们可以优雅地处理。今天你学会了如何让Agent在遇到问题时能'优雅降级'，明天我们将学习如何让Agent'看懂'图片。持续学习，持续进步！"
